1. 主界面的home控件 没放，用的HOME键代替。 主要是页面非常多时，标题后面会产生左右滚动按钮。
放上主页键会遮挡此处按钮。
2. 标题存储， 存储以文件方式存储在config 目录下settings.xml下。
3. 皮肤搞了下，换肤功能做了，但是没有开放，有些资源不全。
4. 金融日历 与 组合管理 的 增删改 左侧的同步用的直接全刷。这里涉及到逻辑比较复杂。如非要局部刷，会比较繁琐。